$kind: http-request
name: create
url: "{{BASE_URL}}/configuration/"
method: POST
body:
  type: json
  content: |-
    {
        "key": "EMAIL_CONTACT",
        "value": "contact@mail.com",
        "description": "Email de contato no rodapé da tela inicial da empresa junior"
    }
auth:
  type: bearer
  credentials:
    token: "{{TOKEN_ADMIN}}"
order: 1777827623793
